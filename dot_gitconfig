[user]
	email = rubenvanstaden@gmail.com
	name = ruben van staden

[init]
    defaultbranch = main

[color]
	ui = true

[core]
	editor = nvim

[alias]
    cm = commit -m
    ca = commit --amend -m
    st = status -sb
    ll = log --graph --oneline --decorate
    last = log -1 head --stat
    cc = checkout
    cb = checkout -b
    bl = branch --list
    bd = branch --delete
    rs = restore --staged *
	df = diff
	ds = diff --staged

	alias = !git config --get-regexp 'alias.*' | colrm 1 6 | sed 's/[ ]/ = /'

[includeif "gitdir:~/codes/github.com/"]
    path = ~/.gitconfig-github

[includeif "gitdir:~/codes/anyonsys.com/"]
    path = ~/.gitconfig-anyon
